int suma(int num1, int num2)
{
	/* Declaraciones*/
	int sum;
	
	/* Argumentos usados aqui*/
	sum = num1+num2;
	
	/* Retorno
	El valor */
	return sum;
}
int main()
{
	int var1, var2, var3;
	
	/* 	Para poder ejecutar el for debemos incluir la funcion generarFor en generar.java
		Similar a generarWhile.
          
          for(int x=99;x<4;x+1)
	{

		x=x+1;
	}
        */
        
        if(i == 1){
            puts(a+b);
        }
        else{
            puts(a-b);
        }
        
        while(true){
            puts(a);
        }
        
        do{
            puts(a);
        }while(a || b);
        
	for(int x=99;x<4;x+1)
	{

		x=x+1;
	}

	gets(var1);
	gets(var2);
	
	int salida;
	var3=salida;
	salida = suma (var1, var2);
	
	puts(var3);
	
	return 0;
}